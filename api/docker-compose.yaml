version: '3.8'
services:
  apis:
    build:
      context: .
      dockerfile: Dockerfile
    command: npm run start:dev
    env_file:
      - .env
    volumes:
      - ./:/app
      - /app/node_modules
    ports:
      - ${PORT}:${PORT}
